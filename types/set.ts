export type Set = {
    setId: number; // the ID of the set within the exercise log
    weight: number; // the weight used for the set
    repetitions: number; // the number of repetitions performed for the set
  };
  
